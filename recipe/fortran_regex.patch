diff --git a/numpy/distutils/fcompiler/gnu.py b/numpy/distutils/fcompiler/gnu.py
index 7832067..6f1428c 100644
--- a/numpy/distutils/fcompiler/gnu.py
+++ b/numpy/distutils/fcompiler/gnu.py
@@ -38,10 +38,10 @@ class GnuFCompiler(FCompiler):
         m = re.search(r'GNU Fortran', version_string)
         if not m:
             return None
-        m = re.search(r'GNU Fortran\s+95.*?([0-9-.]+)', version_string)
+        m = re.search(r'GNU Fortran\s+95.*?\s+([0-9-.]+)', version_string)
         if m:
             return ('gfortran', m.group(1))
-        m = re.search(r'GNU Fortran.*?\-?([0-9-.]+)', version_string)
+        m = re.search(r'GNU Fortran.*?\s+([0-9-.]+)', version_string)
         if m:
             v = m.group(1)
             if v.startswith('0') or v.startswith('2') or v.startswith('3'):
